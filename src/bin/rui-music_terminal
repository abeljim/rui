#!/bin/bash
# Launch ncmpcpp in a terminal with class "music" or with name "Music Terminal"

# st / xst
xst -c music -e ncmpcpp -c ~/.config/ncmpcpp/config
# gnome-terminal --class music -e 'ncmpcpp' 

# Termite
# termite --class music --exec ncmpcpp
